<div class="container-fluid">
  <div id="page-login" class="row">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="col-xs-12 col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3">
      <div class="text-right">
        <a href="<%= new_user_registration_path %>" class="txt-default">Need an account?</a>
      </div>
      <div class="box">
        <div class="box-content">
          <div class="text-center">
            <h3 class="page-header">Ingresar</h3>
          </div>
          <div class="form-group">
            <label class="control-label">Número de Identidad</label>
            <%= f.text_field :id_number, autofocus: true, class: 'form-control' %>
          </div>
          <div class="form-group">
            <label class="control-label">Contraseña</label>
            <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
          </div>
          <div class="text-center">
            <%= f.submit "Ingresar", class: 'btn btn-primary' %>
          </div>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>